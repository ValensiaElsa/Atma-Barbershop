import { Anton, Kanit } from 'next/font/google';
 

export const anton = Anton({ 
    weight: ['400'],
    subsets: ['latin'],});
export const kanit = Kanit({
    weight: ['400'],
    subsets: ['latin'],
  });